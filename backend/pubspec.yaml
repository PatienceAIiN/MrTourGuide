name: mrtouride_backend
description: Local auth backend for MrTouride (Postgres-backed).
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  shelf_static: ^1.1.0
  postgres: ^3.0.0
  crypto: ^3.0.3
  googleapis_auth: ^1.6.0
  http: ^1.2.0
  redis: ^4.0.0
